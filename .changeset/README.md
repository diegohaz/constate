# Changesets

This directory contains changeset files. Each file represents a set of changes that will be included in the next release.

To add a changeset, run:

```bash
pnpm changeset
```

This will prompt you to describe your changes and select which packages are affected.
